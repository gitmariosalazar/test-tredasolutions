class Product{
  constructor({code,name, description, price, stock,createdAt,updatedAt}){
    this.code=code,
    this.name=name,
    this.description=description,
    this.price=price,
    this.stock=stock,
    this.createdAt=createdAt,
    this.updatedAt=updatedAt
  }
}

export default Product;
